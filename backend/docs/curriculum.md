# Curriculum & problems

- YAML is loaded by `curriculum_loader.py`; read model + provider live in `curriculum.py`.
- Generators auto-register from `chapters/`; `_` prefix for helpers. Copy an existing chapter file when adding content.
