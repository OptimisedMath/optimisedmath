# Docstrings

Reference: [session_state.py](backend/session_state.py).

**Add** for route handlers, multi-branch logic, public helpers with side effects, API models, and module docstrings on entry points. **Skip** private `_helpers` under ~5 lines, trivial one-liners.

For Topic generators add polish one-liners and add short comments that describe Traps. Reference: [topic_10_zamiana.py](backend/chapters/ulamki_dziesietne/topic_10_zamiana.py)

Default style: imperative one-liner; add `Args` / `Returns` / `Raises` when helpful. English for infrastructure; Polish for problem generators.